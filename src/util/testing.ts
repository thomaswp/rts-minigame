import * as Matter from 'matter-js';
// let bodies = 
// let collisionDetector = Matter.Detector.create()

// let bodies = objects
// Matter.Detector.setBodies(collisionDetector, bodies)

// Matter.Detector.collisions(collisionDetector)

// // each physics object could get the list of objects from the world, filter it to physics objects, then get each body from each physics object, and then use query on that list of bodies
// Matter.Query.collides()